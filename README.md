# Git

```
git log --graph --oneline
git push origin --delete <branchName>
git branch -d <branchName>
git clean -d -x -n
git log --graph --oneline --color
git rebase -i origin/develop
git reset --soft HEAD~1
git reset --hard origin/master  (re checkout branch)

```
